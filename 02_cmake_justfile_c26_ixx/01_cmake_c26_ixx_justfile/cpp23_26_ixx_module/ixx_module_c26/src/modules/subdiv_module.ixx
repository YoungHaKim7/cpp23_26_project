export module subdiv_module;

export namespace subdiv_api {
    inline int subtract(int left, int right) {
        return left - right;
    }

    inline int divide(int left, int right) {
        return left / right;
    }
}
